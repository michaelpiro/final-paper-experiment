pca_dim: 5
hidden_dims: [32,32]
sigma_values: [0.01]
amplitude: 0.15
target_fraction: 0.1
total_samples: 3000        # total background samples across all classes
test_fraction: 0.2         # per-class fraction held out for test
base_epochs: 2000
lr: 0.001
weight_decay: 0.0001
delta_theta: 0.01        # step size for LFI central-difference Jacobian (LRao paper)
batch_size: 512
results_dir: experiments/multiclass_amp0.15_sigma0.01_hidden_dims_256_256_real_glrt
